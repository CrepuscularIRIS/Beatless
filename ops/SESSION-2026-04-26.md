# Session Log — 2026-04-26

## Commits this session (b3aa50c → 6023ce3, 13 commits)

| SHA | Type | Summary |
|-----|------|---------|
| `b3aa50c` | fix(plan) | patch all stale ~/blog/ refs to ~/claw/blog/ |
| `caa2ed0` | chore | gitignore local orphan-blog archives + record cleanup |
| `49c9a56` | feat(github-response) | CI-debug pipeline with retry escalation ladder |
| `a0eebd6` | plan | add TODO-9 — local PDF→Markdown via MinerU |
| `1ad8f6b` | feat(paper-fulltext) | v0 PDF→Markdown stage closes the KB-foundation loop |
| `ece54c4` | plan | enshrine 3 Core Research Principles + dual-backend paper-fulltext |
| `b61a67c` | feat(research-host) | hosted autonomous pipeline + retire full-PDF path |
| `18eada3` | feat(cron) | research-host-cron + system-capacity doc |
| `6023ce3` | fix(github-pr) | 4 preflight gates + auto-remediation per StatPan/lawpy #5 retrospective |

## Headline deliverables

### 1. Three Core Research Principles enshrined
Per user directive — written and underlined in `~/claw/plan/Regulations.md`:
- **P1** Parallel + Orthogonal Coverage (≥3 niches, cosine ≤ 0.6, R11 entropy collapse)
- **P2** Triple-Heterogeneous Review (Codex / Gemini / Sonnet-fresh; no family judges itself)
- **P3** Surface Implicit Knowledge (silent_priors / unspoken_alternatives / failure_dna /
       hidden_dependencies / what_a_skeptical_PI_would_ask, each with evidence_pointer)

Embedded into `/research-loop`, `/research-parallel`, `/research-review` commands.

### 2. /research-host meta-command (~/.claude/commands/research-host.md)
End-to-end hosted autonomous pipeline. One command runs Phase 0 (status/resume) →
Phase 1 (discover, two-path) → Phase 2 (bootstrap + orthogonality) → Phase 3 (parallel
dispatch + mutation operators + QD archive) → Phase 4 (triple review) → Phase 5 (reflect)
→ loop until one of 6 enumerated halt conditions.

Grounded in `~/research/Report/AI 科研系统演化新范式.md`:
- Rules = selection pressure
- Agents = mutation sources (3 operators: semantic / non-uniform / trajectory-grounded)
- Quality-Diversity over single-objective (MAP-Elites archive, stepping stones)

### 3. research-host-cron.py — Hermes cron driver
Detects active research workspace, gathers state, fires `claude -p /research-host` with
stdin closed (codex hangs without it). Refuses to fire if codex/gemini are dead.
Registered as Hermes cron job `fe550514fa1e` running every 360m.

### 4. github-pr.py — 4 preflight gates + auto-remediation
Triggered by StatPan/lawpy PR #5 retrospective:
- Gate 2 NEW: bot-author filter (rejects `app/github-actions`, `[bot]`, etc.)
- Gate 3 NEW: bot-noise label filter (`api-drift`, `ci-failure`, `automated`)
- Gate 4 NEW: MIN_REPO_STARS=20 default (env-overridable)
- Auto-remediation: status=pr-created-unreviewed → auto-close PR with apology

### 5. Blog rebalance (5-hIE taxonomy go-live)
- Navbar switched to Papers / Signals / Audit / Build / Digest (Kimi-named)
- 191 manually-curated essays un-hidden + backfilled hIE field
- 117 hero_image strings converted to heroImage object form (now render)
- 55 newly-translated ZH pairs synced + pushed (cb28798)
- Final visible distribution: methode 91, kouka 68, snowdrop 45, lacia 25, saturnus 10

### 6. Zotero→Obsidian fix
ZOTERO_API_KEY + ZOTERO_USER_ID were missing from ~/.hermes/.env (cron loads from there,
not ~/claw/.env). Sync had been silently broken for 3 days. After patch, vault grew from
24 → 924 → 10,324 paper notes in one cron tick.

### 7. Codex/Gemini multi-turn verified
Codex shell-tool verified: read magic 99887766 from disk, performed sed fix, re-ran tests,
returned "ALL TESTS PASSED". Gemini WebFetchTool verified: returned correct arXiv URL +
abstract first sentence for the 2022 Anthropic CAI paper.

Critical gotcha: codex exec hangs on "Reading additional input from stdin..." when invoked
from a subprocess with stdin not closed. Fix: always pass `</dev/null` or
`stdin=subprocess.DEVNULL`. Documented in `/research-host.md` and research-host-cron.py.

### 8. PR #5 retrospective + closure
StatPan/lawpy PR #5 closed with apology. Misalignments documented:
- Issue #2 was bot-generated API drift alert, not a feature request
- "Closes #2" framing misled the maintainer
- Triple-review chain didn't run before PR went live (constitutional violation)

## Cron lineup (11 active jobs as of session end)

```
every  10m  Blog Translate          every 360m  Research Host (NEW)
every  30m  Blog Draft              every 360m  Paper Harvest
every  60m  GitHub Response         every 360m  Zotero Sync
every 150m  GitHub PR Pipeline      every 720m  Feed Crawl
every 240m  Auto Research (legacy)  every 720m  Blog Maintenance
                                    every 1440m Daily Evolution
```

Snapshot of `hermes cron list` output: `ops/cron-registry-2026-04-26.txt`.

## Open follow-ups

- Auto-research (legacy `/exp-run` driver) can be retired — `/research-host` covers same
  ground with better discipline. Decision deferred.
- MinerU + paper-fulltext.py left as on-demand only (user direction: lightweight feed/digest
  is enough for current needs).
- First natural fire of Research Host: 2026-04-26T15:09 EDT.
- Saturnus 5-hIE asset directory still empty (TODO-1, TODO-2 in `plan/blog-hIE-TODO.md`).
