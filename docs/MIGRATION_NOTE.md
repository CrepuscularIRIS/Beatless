# Migration Note

This reset intentionally removes legacy `rawcli-experiments` governance scripts and workflows.
The old scheduled failure on Actions run `23933293737` belonged to the removed legacy stack.

Current repository tracks the new OpenClaw 5-MainAgent baseline only.
