---
description: Hermes-native cron job skills — replacements for the legacy ~/.hermes/scripts/*.py drivers. Each skill is invoked via `hermes chat -Q -s <name> -q "<prompt>"` from a thin cron shim. Skills compose with routing/* skills (claude-code-router / codex-router / gemini-router) for heterogeneous review.
---
